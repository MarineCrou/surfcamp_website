// Getting an article from our parent componnet, which is the blog article componente.

const ArticleIntro = ({ article }) => {
  console.log(article);
  return <div className="article-intro"></div>;
};

export default ArticleIntro;
